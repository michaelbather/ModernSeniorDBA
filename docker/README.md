# Docker SQL Server Lab

This folder contains the Docker Compose configuration used to create a local SQL Server 2022 development environment.

## Start

docker compose up -d

## Stop

docker compose down

## Reset

docker compose down -v
docker compose up -d
